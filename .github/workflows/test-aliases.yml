name: test-aliases

on:
  push:

jobs:

  smote_test:    
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - uses: actions/setup-node@v1
      with:
        node-version: 12
    
    - name: Try aliases
      run: |
        alias hello="echo hello"
        alias
        hello    
